_target_: emg2qwerty.lightning.CNNRNNCTCModule

in_features: 528
mlp_features: [384]

conv_channels: 256
kernel_size: 5
num_conv_layers: 2

rnn_hidden_size: 256
rnn_num_layers: 1
bidirectional: true
dropout: 0.1

optimizer: ${optimizer}
lr_scheduler: ${lr_scheduler}
decoder: ${decoder}
